<form method="post" action="{{ path('app_admin_article_type_delete', {'id': article_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article_type.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
