<form method="post" action="{{ path('app_admin_conference_delete', {'id': conference.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ conference.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
