<form method="post" action="{{ path('app_admin_tutoring_delete', {'id': tutoring.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tutoring.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
